export default function createReportObject(employeesList) {
  const officeReport = {
    allEmployees:  { ...employeesList };

    getNumberOfDepartments(allEmployeesProperty) {
      // return no of keys in employeesList
      let obj_no = 0;
      for (const value of allEmployeesProperty) {
	obj_no += 1;
      };

      return obj_no;
    },
  };

  return officeRepprt
}
